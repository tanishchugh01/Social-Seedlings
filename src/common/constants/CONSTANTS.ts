export const CONSTANTS = {
    // API
    API: {
        UNSPLASH: {
            BASE_URL: "https://api.unsplash.com",
            
            PAGE_LIMIT: 10,
            page_random:  Math.floor(Math.random() * 100) + 1,
        }
    },
    
    // ROUTES
    ROUTES: {
        HOME: "/",
        PROFILE: "/user",
    }
};
    
    